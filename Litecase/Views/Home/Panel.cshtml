<div class="navbar navbar-inverse navbar-fixed-top navbar" style="background-color: #706651">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("LITECASE.BY", "Index", "Home", null, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav pull-left">
                <li>@Html.ActionLink("О проекте", "About", "Home")</li>
                <li>@Html.ActionLink("Контакты", "Contact", "Home")</li>
            </ul>
            <ul class="nav navbar-nav pull-right">
                @if (ViewBag.mail == null)
                {   
                    <li><a href="@Url.Action("Register", "Home")"><span class="glyphicon glyphicon-lock"> Регистрация</span></a></li>
                    <li><a href="@Url.Action("Login", "Home")"><span class="glyphicon glyphicon-log-in"> Вход</span></a></li>
                }
                else
                {
                    string mail = ViewBag.mail;
                    <li><a href="@Url.Action("RoomUser", "Home")"><span class="glyphicon glyphicon-user"> mail</span></a></li>
                    <li><a href="@Url.Action("Exit", "Home")"><span class="glyphicon glyphicon-log-out"> Выход</span></a></li>
                }
            </ul>
        </div>
    </div>
</div>